<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Cold-start splash via the Android 12+ SplashScreen API (backported to
         older releases by androidx core-splashscreen). Brand mark on a flat
         near-black background — identical in light and dark, so there is never a
         white flash. `postSplashScreenTheme` hands off to NormalTheme once the
         Flutter engine is ready, avoiding a second (Flutter) splash. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>

    <!-- The window behind the Flutter UI once it is running (light mode). -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
