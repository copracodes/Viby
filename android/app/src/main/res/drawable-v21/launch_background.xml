<?xml version="1.0" encoding="utf-8"?>
<!-- Fallback window background during launch (API 21+). Flat brand near-black,
     matching the SplashScreen background so there is no white flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
