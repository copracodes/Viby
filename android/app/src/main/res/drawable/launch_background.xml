<?xml version="1.0" encoding="utf-8"?>
<!-- Fallback window background during launch. Flat brand near-black (not white),
     so nothing flashes before the SplashScreen icon / Flutter UI appears. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
