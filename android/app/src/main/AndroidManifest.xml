<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================ PERMISSIONS ============================ -->

    <!-- Streaming (Subsonic), artwork downloads (dio / cached_network_image),
         online lyrics (LRCLIB). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Read the active network's metered/transport type for the online-lyrics
         "Wi-Fi only" gate (ConnectivityManager). No network traffic itself. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Run audio_service as a foreground service so playback survives when the
         app is backgrounded. Required from Android 9 (API 28). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 14 (API 34+) requires the specific foreground-service-type
         permission that matches the service's declared type (mediaPlayback). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Keep the CPU awake during playback with the screen off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Read on-device audio for the local library. Android 13+ (API 33) uses
         the granular media permission. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- on_audio_query_pluse's permission gate requires BOTH READ_MEDIA_AUDIO
         and READ_MEDIA_IMAGES on Android 13+ (it hardcodes the pair); without
         images its querySongs path errors and the plugin double-replies,
         crashing the app. So we must declare + request images too. Revisit if
         the fork narrows this to audio-only. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Legacy storage read for Android 12L and below (API <= 32), where the
         granular READ_MEDIA_AUDIO permission does not exist. Capped with
         maxSdkVersion so it is not requested on 33+. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:label="Viby"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- audio_service background playback service. `mediaPlayback` type
             pairs with the FOREGROUND_SERVICE_MEDIA_PLAYBACK permission above so
             notifications/playback continue when the app is not foregrounded.
             exported=true lets the system media session bind to it. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Routes hardware/Bluetooth media buttons (play/pause/next) to
             audio_service. Required by the plugin for media-button handling. -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Fall back to the Skia rendering backend. On this device's Vulkan
             driver (Flutter 3.41 / Impeller-Vulkan) Material icon-font glyphs
             render as ".notdef" tofu boxes and the InkSparkle shader asset is
             reported missing. Skia renders them correctly. Revisit (remove this
             flag) once Impeller/Vulkan glyph rendering is fixed for this GPU. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
